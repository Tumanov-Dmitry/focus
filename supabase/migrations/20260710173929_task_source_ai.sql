-- Источник задачи для ИИ-слоя: задачи, предложенные ИИ и подтверждённые
-- пользователем, помечаются source = 'ai'. Изменение аддитивное — существующие
-- строки с 'manual'/'inbox' не затрагиваются.
alter type public.task_source add value if not exists 'ai';
